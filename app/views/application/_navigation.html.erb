<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
  <div class="container">
    <a class="navbar-brand" href="#">Ruby Lovers</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="<%= root_path %>">Home
            <span class="sr-only">
              <% if current_user %>
                 (<%= current_user.email %>)
              <% end  %>
            </span>
          </a>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active px-1">
          <% if @cart %>
            <%= link_to 'Cart', cart_path(@cart), class:"nav-link btn btn-outline-secondary btn-inverse" %>
        <% end  %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item px-1">        <%= link_to 'Edit profile', edit_user_registration_path , class:"nav-link btn btn-outline-secondary btn-inverse" %></li>
          <li class="nav-item active px-1"> <%= link_to "Log Out", destroy_user_session_path, method: :delete, data: {confirm: 'Are you sure'}, class:"nav-link btn btn-outline-secondary btn-inverse" %></li>
      <% else %>
          <li class="nav-item px-1">        <%= link_to 'Log In', new_user_session_path , class:"nav-link btn btn-outline-secondary btn-inverse" %></li>
          <li class="nav-item active px-1"> <%= link_to 'Sign Up', new_user_registration_path, class:"nav-link btn btn-outline-secondary btn-inverse" %></li>
      <% end  %>
      </ul>
    </div>
  </div>
</nav>